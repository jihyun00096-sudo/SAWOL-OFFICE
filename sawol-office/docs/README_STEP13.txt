STEP 13 파일 구성

1. 13_AUTH_RLS_GUIDE.md
   - 대표 계정 생성부터 RLS 검증까지 전체 설명

2. 13_AUTH_RLS.sql
   - Supabase SQL Editor 실행용
   - 실행 전 반드시 대표 Supabase Auth User UUID를 교체해야 함

주의:
- 비밀번호를 SQL에 넣지 않는다.
- Service Role Key를 넣지 않는다.
- 대표 UUID만 교체한다.
- STEP 9/12가 완료된 DB에서 실행한다.
